To get node information for queue definitions:

> scontrol show node

Look for CPUTot and RealMemory. Max memory would be RealMemory



    # Does the SURLM cluster use constraints to specify GPU types?
    slurm_constraints: True
